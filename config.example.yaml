sip:
  udp_addr: "0.0.0.0:5060"
  tcp_addr: "0.0.0.0:5060"
  # Uncomment to enable TLS/WSS (paths are relative to working directory):
  # tls_addr: "0.0.0.0:5061"
  # wss_addr: "0.0.0.0:5081"
  # tls_cert: "certs/cert.pem"
  # tls_key:  "certs/key.pem"
  dns: "8.8.8.8"
  disable_auth: false

pprof:
  # Never expose pprof to 0.0.0.0 in production.
  addr: "127.0.0.1:6658"

push:
  apns_cert: "./voip-callkeep.p12"
  fcm_service_account: "service-account.json"
